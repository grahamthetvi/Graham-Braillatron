# LocalSend receive-only sidecar (HTTP auto-accept)
enabled=true
listen_port=53317
alias=Braillatron
# Encryption off — phone LocalSend must use HTTP / encryption disabled,
# or add this device manually by IP on port 53317.
protocol=http
auto_accept=true
