# Braillatron Bluetooth output via BlueALSA (A2DP). Requires paired device MAC.
# Switch output: sudo braillatron-audio-select aux|bluetooth|i2s
pcm.!default {
  type plug
  slave.pcm {
    type bluealsa
    device "@MAC@"
    profile "a2dp"
  }
}

ctl.!default {
  type bluealsa
}
