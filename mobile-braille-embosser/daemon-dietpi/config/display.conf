# Visual display backend — ST7789 SPI panel on HAT
backend=auto

spidev=/dev/spidev0.0
width=240
height=240

# Placeholder GPIO lines until HAT schematic is finalized (-1 = unconfigured)
gpio_dc=-1
gpio_rst=-1
gpio_cs=-1

ncurses_enabled=true
